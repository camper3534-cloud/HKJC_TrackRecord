# HKJC Track Record（公開往績）

每個賽日**開跑前**公開模型頭三。時間戳由 GitHub 蓋，公開之後唔會靜靜雞改。

## 點睇

| 資料夾 | 算唔算往績 |
|---|---|
| `predictions/` | **算**。正式開跑前公開 |
| `pipeline_test/` | **唔算**。只係試通推送管道 |

往績清單見 `manifest.json`（只計 `predictions/`）。
